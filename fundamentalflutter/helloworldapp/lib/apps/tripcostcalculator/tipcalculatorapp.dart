import 'package:flutter/material.dart';
import './tipcalcualorhome.dart';

class TripCostCalculator extends StatelessWidget {
  Widget build(BuildContext buildContext) {
    return MaterialApp(
      title: 'Trip Cost Calculator',
      home: TripCostHome(),
    );
  }
}
